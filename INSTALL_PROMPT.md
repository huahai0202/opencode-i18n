# One-Prompt Install

把下面这一句发给 OpenCode：

```text
运行 opencode plugin opencode-i18n 安装 OpenCode i18n 插件，完成后告诉我重启 OpenCode，并运行 /i18n 选择 English、简体中文或繁體中文。
```

或者按命令面板方式：按 Ctrl+P 打开命令面板，搜索 install plugin，输入 opencode-i18n，回车安装。
